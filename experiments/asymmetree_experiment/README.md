## Experiment 3: calibrating the costs of a reconciliation tool

A small-scale version of the experiment can be executed (provided **dependencies** are installed by simply running:
```
make
```
The complete version can be executed with:
```
make complete
```
